# ── LangGraph + LangChain ─────────────────────────────────────────────────────
langgraph>=1.2.0                  # LangGraph 1.0+ — master StateGraph, subgraphs
langchain>=1.3.0                   # create_agent, before_model middleware
langchain-openai>=1.2.1            # OpenAI-compatible LLM provider
langchain-core>=1.4.0              # tools, messages, base classes
langgraph-checkpoint-mongodb>=0.4.0 # MongoDB checkpointer for LangGraph state

# ── FastAPI ───────────────────────────────────────────────────────────────────
fastapi>=0.136.1                   # Web framework — QuickSuite standard
uvicorn>=0.47.0                    # ASGI server
python-multipart>=0.0.28          # Form data support

# ── HTTP Client ───────────────────────────────────────────────────────────────
httpx>=0.28.1                       # Async HTTP — Vector API calls

# ── MongoDB ───────────────────────────────────────────────────────────────────
# pymongo 4.9+ has native async support via AsyncMongoClient — motor is no longer needed.
# Use AsyncMongoClient for all service layer code (user_service, conversation_service etc.)
# Use MongoDBSaver / AsyncMongoDBSaver from langgraph-checkpoint-mongodb for checkpointing
pymongo>=4.17.0                      # Native async via AsyncMongoClient — replaces motor

# ── Auth ──────────────────────────────────────────────────────────────────────
PyJWT>=2.12.1                       # JWT decode + validation
cryptography>=48.0.0               # RS256 signature verification

# ── Config + Validation ───────────────────────────────────────────────────────
pydantic>=2.13.4                     # Data models, request/response schemas
pydantic-settings>=2.14.1          # Settings from .env file
python-dotenv>=1.2.2              # Load .env for local dev

# ── Redis ─────────────────────────────────────────────────────────────────────
# Async pub/sub via redis.asyncio — replaces deprecated aioredis
# Used for prompt cache invalidation across AKS pods
redis>=7.4.0